/** Published release metadata unavailable from the GitHub Releases API body. */
export const releaseMetadataByTag: Readonly<Record<string, { versionCode?: number }>> = {
  'v1.0.0': { versionCode: 3 },
}
